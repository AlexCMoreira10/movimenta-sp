<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <title>Lista de Ônibus</title>
</head>
<body>
  <h1>Lista de Ônibus - Capelinha</h1>

  {{#if onibus.length}}
    <table border="1" cellpadding="10">
      <thead>
        <tr>
          <th>Código da Linha</th>
          <th>Número da Linha</th>
        </tr>
      </thead>
      <tbody>
        {{#each onibus}}
          <tr>
            <td>{{codigo_linha}}</td>
            <td>{{numero_linha}}</td>
          </tr>
        {{/each}}
      </tbody>
    </table>
  {{else}}
    <p>Nenhum ônibus encontrado.</p>
  {{/if}}
</body>
</html>
